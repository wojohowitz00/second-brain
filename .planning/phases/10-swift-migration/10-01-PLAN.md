# Phase 10.1: Swift Migration - Foundation

**Goal**: Initialize the native macOS project and port the critical backend logic (Ollama communication and Vault scanning) to Swift.

## User Review Required

> [!IMPORTANT]
> **New Directory**: A new `apple/` directory will be created at the project root to house the Xcode project, keeping it separate from the legacy `backend/` Python code during the transition.

## Proposed Changes

### Project Initialization
#### [NEW] [apple/SecondBrain/](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/)
- Create a new Xcode project structure (SwiftUI App Life Cycle).
- Configure `Info.plist` for local network access (Ollama).
- Set up Testing targets (`SecondBrainTests`).

### Core Logic Porting
#### [NEW] [apple/SecondBrain/Services/OllamaClient.swift](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/Services/OllamaClient.swift)
- **Class**: `OllamaClient`
- **Methods**: `isReachable()`, `generate(prompt:)`, `listModels()`.
- **Implementation**: Use `URLSession` (`async/await`) to hit `http://localhost:11434`.

#### [NEW] [apple/SecondBrain/Services/VaultScanner.swift](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/Services/VaultScanner.swift)
- **Class**: `VaultScanner`
- **Methods**: `scanDomains(root: URL) -> [Domain]`.
- **Implementation**: Use `FileManager` to traverse PARA directories and build vocabulary.

#### [NEW] [apple/SecondBrain/Models/](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/Models/)
- `Domain.swift`, `ParaType.swift`, `Note.swift` (Codable structs matching the Python schema).

## Verification Plan

### Automated Tests (XCTest)
*   **OllamaTests**:
    *   Mock `URLProtocol` to verify request formatting and JSON parsing.
    *   Integration test: Hit real local Ollama (if running) to verify `isReachable`.
*   **VaultScannerTests**:
    *   Create temporary directory structure in `setUp`.
    *   Verify scanner correctly identifies Domains, PARA folders, and Subjects.

### Manual Verification
*   **Build**: Ensure project builds without errors in Xcode (or via `xcodebuild`).
*   **Run**: Launch app, verify "Hello World" (or basic status) and console logs showing successful connection to Ollama.
