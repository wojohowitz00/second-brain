---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/hooks/vault-write-guard.py
  - .claude/settings.json
autonomous: true

must_haves:
  truths:
    - "Any Write or Edit targeting 01_Projects, 02_Areas_of_Interest, 03_Research, or 04_Archive in the vault is blocked with exit code 2"
    - "Blocked attempts produce a clear stderr message explaining the violation and where to write instead"
    - "Writes to 05_AI_Workspace/ and all non-vault paths proceed without interference"
    - "Bash commands containing vault write patterns to blocked folders are blocked (best-effort)"
    - "Hook is registered in settings.json (committed) not settings.local.json (machine-local)"
  artifacts:
    - path: ".claude/hooks/vault-write-guard.py"
      provides: "PreToolUse hook script that blocks vault writes to human PARA folders"
      min_lines: 25
      contains: "sys.exit(2)"
    - path: ".claude/settings.json"
      provides: "Hook registration matching Write|Edit|Bash tools"
      contains: "PreToolUse"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/vault-write-guard.py"
      via: "hooks.PreToolUse[].hooks[].command references the script path"
      pattern: "vault-write-guard"
---

<objective>
Implement a PreToolUse hook that blocks Claude from writing to human PARA folders (01-04) in the Obsidian vault.

Purpose: Enforce the vault purity constraint — AI never writes to human PARA folders. This is the safety boundary that protects existing vault content from accidental AI modifications.

Output: A Python hook script at `.claude/hooks/vault-write-guard.py` and hook registration in `.claude/settings.json`.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the vault-write-guard.py hook script</name>
  <files>.claude/hooks/vault-write-guard.py</files>
  <action>
Create `.claude/hooks/` directory and write `vault-write-guard.py`.

The script must:

1. Read JSON from stdin (Claude provides `tool_name`, `tool_input` in the payload)
2. Define `VAULT_ROOT = "/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home"`
3. Define `BLOCKED_PREFIXES` list: `01_Projects`, `02_Areas_of_Interest`, `03_Research`, `04_Archive` (all under VAULT_ROOT)
4. For Write, Edit, MultiEdit tools: extract `file_path` from `tool_input`, check if it starts with any blocked prefix
5. For Bash tool: extract `command` from `tool_input`, check if command contains a blocked prefix AND a write-indicating pattern (regex: `>|>>|\btee\b|\bcp\b|\bmv\b|\btouch\b|\bmkdir\b`)
6. If blocked: print explanation to stderr and `sys.exit(2)`
7. If allowed: `sys.exit(0)`

The stderr message on block must include:
- "VAULT WRITE BLOCKED" header
- The attempted path or command
- "AI writes must target 05_AI_Workspace/ only."

Add a module docstring explaining the hook's purpose, exit code semantics (0=proceed, 2=block), and that Bash blocking is best-effort.

Use the exact implementation from the research file (`.planning/phases/01-foundation/01-RESEARCH.md` lines 383-429) — it has been verified against official Claude Code hook documentation.

Make the script executable: `chmod +x .claude/hooks/vault-write-guard.py`
  </action>
  <verify>Run `python3 .claude/hooks/vault-write-guard.py` with test JSON piped via stdin. Test cases:
  - `echo '{"tool_name":"Write","tool_input":{"file_path":"/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/01_Projects/test.md"}}' | python3 .claude/hooks/vault-write-guard.py` — should exit 2
  - `echo '{"tool_name":"Write","tool_input":{"file_path":"/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/test.md"}}' | python3 .claude/hooks/vault-write-guard.py` — should exit 0
  - `echo '{"tool_name":"Write","tool_input":{"file_path":"/Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/README.md"}}' | python3 .claude/hooks/vault-write-guard.py` — should exit 0 (non-vault path)
  </verify>
  <done>vault-write-guard.py exists, is executable, blocks writes to 01-04 vault folders, allows writes to 05_AI_Workspace and non-vault paths.</done>
</task>

<task type="auto">
  <name>Task 2: Create settings.json with hook registration</name>
  <files>.claude/settings.json</files>
  <action>
Create `.claude/settings.json` (new file — separate from existing `settings.local.json`).

Contents:
```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 $CLAUDE_PROJECT_DIR/.claude/hooks/vault-write-guard.py"
          }
        ]
      }
    ]
  }
}
```

Key details:
- Use `$CLAUDE_PROJECT_DIR` environment variable so the path resolves correctly regardless of cwd
- Matcher covers Write, Edit, and Bash (MultiEdit is matched by Edit pattern)
- Do NOT modify the existing `settings.local.json` — it handles permissions separately
  </action>
  <verify>Read `.claude/settings.json` to confirm valid JSON with correct hook configuration. Confirm `.claude/settings.local.json` is unchanged.</verify>
  <done>settings.json exists with PreToolUse hook registration. settings.local.json is untouched.</done>
</task>

</tasks>

<verification>
1. Run the three test cases from Task 1 verify section — confirm exit codes 2, 0, 0 respectively
2. Read `.claude/settings.json` — confirm valid JSON with PreToolUse hook pointing to vault-write-guard.py
3. Read `.claude/settings.local.json` — confirm it is unchanged (still contains only permissions)
4. Run `ls -la .claude/hooks/vault-write-guard.py` — confirm file exists and is executable
</verification>

<success_criteria>
- vault-write-guard.py blocks writes to all four human PARA folders (01-04) with exit code 2 and stderr explanation
- vault-write-guard.py allows writes to 05_AI_Workspace/ and non-vault paths with exit code 0
- settings.json registers the hook for Write|Edit|Bash tools via PreToolUse
- settings.local.json is not modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
