---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/phases/01-foundation/yaml-frontmatter-schema.md
  - "/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/dashboards/tasks-by-status.md"
  - "/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/CLAUDE.md"
autonomous: true

must_haves:
  truths:
    - "A canonical YAML frontmatter schema document defines field names, types, allowed values, and examples for task, project, and person note types"
    - "The schema preserves all existing template fields (type, title, due_date, status, tags, created, name, next_action, context, follow_ups, last_touched)"
    - "Date fields are specified as bare ISO format (no quotes) for Dataview date type inference"
    - "A skeleton Dataview dashboard exists in 05_AI_Workspace/dashboards/ that renders a live query"
    - "The schema document is referenced from BOTH the vault-level CLAUDE.md (05_AI_Workspace/CLAUDE.md) AND the project CLAUDE.md (.claude/claude.md)"
  artifacts:
    - path: ".planning/phases/01-foundation/yaml-frontmatter-schema.md"
      provides: "Canonical YAML frontmatter schema for tasks, projects, and people"
      min_lines: 60
      contains: "type: task"
    - path: "/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/dashboards/tasks-by-status.md"
      provides: "Skeleton Dataview dashboard with at least one live query"
      contains: "dataview"
  key_links:
    - from: ".claude/claude.md"
      to: ".planning/phases/01-foundation/yaml-frontmatter-schema.md"
      via: "Reference or mention in project CLAUDE.md pointing to schema location"
      pattern: "yaml-frontmatter-schema"
    - from: "/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/CLAUDE.md"
      to: ".planning/phases/01-foundation/yaml-frontmatter-schema.md"
      via: "Reference in vault CLAUDE.md pointing to schema location"
      pattern: "yaml-frontmatter-schema"
---

<objective>
Define the canonical YAML frontmatter schema for tasks, projects, and people notes, and create a skeleton Dataview dashboard that proves the schema works with live queries.

Purpose: Lock in field names, types, and allowed values BEFORE any skill writes structured data. This prevents schema drift and ensures every AI-generated note is Dataview-queryable from day one.

Output: A schema document at `.planning/phases/01-foundation/yaml-frontmatter-schema.md` and a working dashboard at `05_AI_Workspace/dashboards/tasks-by-status.md`.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.claude/claude.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical YAML frontmatter schema document</name>
  <files>.planning/phases/01-foundation/yaml-frontmatter-schema.md</files>
  <action>
Create `.planning/phases/01-foundation/yaml-frontmatter-schema.md` with the canonical schema.

The document must contain:

**Header and purpose:**
- Title: "Canonical YAML Frontmatter Schema"
- Statement: This is the single source of truth for YAML frontmatter field names, types, and allowed values used across the Obsidian vault.
- Note: Schema is additive over existing templates — all existing fields are preserved.

**Schema for Tasks (`type: task`):**
```yaml
type: task                    # Required. Literal "task"
title: ""                     # Required. Free text
due_date: 2026-03-14          # Optional. ISO date, NO QUOTES (Dataview types as date)
status: backlog               # Required. Enum: backlog | active | waiting | blocked | done | someday
priority: medium              # Optional. Enum: high | medium | low
project: "[[ProjectName]]"    # Optional. Obsidian wikilink (quoted in YAML, Dataview reads as link)
domain: admin                 # Optional. Enum: sales | content | product | admin | research | people
context: ""                   # Optional. Enum: quick | deep | collab
tags: []                      # Optional. Array of strings
created: 2026-03-14           # Required. ISO date, NO QUOTES
```

**Schema for Projects (`type: project`):**
```yaml
type: project                 # Required. Literal "project"
name: ""                      # Required. Free text
status: active                # Required. Enum: active | on-hold | done | archived
deadline: 2026-06-01          # Optional. ISO date, NO QUOTES
health: green                 # Optional. Enum: green | yellow | red
domain: admin                 # Optional. Enum: sales | content | product | admin | research | people
next_action: ""               # Optional. Free text — next concrete step
tags: []                      # Optional. Array of strings
created: 2026-03-14           # Required. ISO date, NO QUOTES
```

**Schema for People (`type: person`):**
```yaml
type: person                  # Required. Literal "person"
name: ""                      # Required. Free text
relationship: ""              # Optional. Enum: client | friend | colleague | vendor | other
last_contact: 2026-03-14      # Optional. ISO date, NO QUOTES
follow_up_date: 2026-04-01    # Optional. ISO date, NO QUOTES — drives CRM alerts
context: ""                   # Optional. Free text
follow_ups: []                # Optional. Array of strings
tags: []                      # Optional. Array of strings
last_touched: 2026-03-14      # Keep for backward compat with existing templates
```

**Dataview typing rules section:**
- Bare ISO dates (`2026-03-14`) are typed as date — enables date comparisons
- Quoted ISO dates (`"2026-03-14"`) are typed as TEXT — breaks date queries. NEVER quote dates.
- Wikilinks (`"[[Name]]"`) must be quoted in YAML but Dataview reads them as link type
- Empty strings (`""`) are valid — Dataview treats as null/empty
- Arrays (`[]`) are valid — Dataview supports list operations

**Enum reference table:** Consolidate all enums (status, priority, domain, context, relationship, health) into a quick-reference table.

**Backward compatibility note:** Existing templates use `due_date`, `status`, `tags`, `type`, `created`. This schema adds `priority`, `project`, `domain`, `context` (tasks), `deadline`, `health`, `domain` (projects), `relationship`, `last_contact`, `follow_up_date` (people). No existing fields are removed or renamed.
  </action>
  <verify>Read the schema document. Confirm it contains all three note types with full field definitions, typing rules, and enum reference.</verify>
  <done>Schema document exists with task, project, and person schemas. All existing fields preserved. Typing rules documented. Enum reference included.</done>
</task>

<task type="auto">
  <name>Task 2: Create skeleton Dataview dashboard and add schema reference to CLAUDE.md</name>
  <files>
    /Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/dashboards/tasks-by-status.md
    .claude/claude.md
    /Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/CLAUDE.md
  </files>
  <action>
**Part A: Create the dashboard note.**

Write `tasks-by-status.md` in the vault's `05_AI_Workspace/dashboards/` folder.

The dashboard must contain:
- YAML frontmatter: `type: dashboard`, `created: [today's date bare ISO]`, `refreshed_by: claude`
- Title: "# Active Tasks by Status"
- Description paragraph explaining this is an auto-generated Dataview dashboard
- At least one Dataview DQL query block:

```dataview
TABLE without id
  file.link AS Task,
  status AS Status,
  priority AS Priority,
  due_date AS Due,
  project AS Project
FROM "tasks"
WHERE type = "task" AND status != "done"
SORT due_date ASC
```

- A second query showing tasks grouped by status:

```dataview
TABLE without id
  file.link AS Task,
  priority AS Priority,
  due_date AS Due
FROM "tasks"
WHERE type = "task"
GROUP BY status
SORT status ASC
```

- Footer note: "This dashboard is auto-generated by Claude. Do not edit manually."

**Part B: Add schema reference to project CLAUDE.md.**

Append a new section to `.claude/claude.md` (the project-level CLAUDE.md, NOT the vault CLAUDE.md files):

```
## YAML Frontmatter Schema

Canonical schema: `.planning/phases/01-foundation/yaml-frontmatter-schema.md`

All AI-generated notes with structured data MUST use field names and types defined in the canonical schema. Key rules:
- Date fields: bare ISO format (no quotes) — e.g., `due_date: 2026-03-14`
- Status enums: use only values defined in schema
- New fields: must be added to schema before use
```

IMPORTANT: Use Edit tool to append to claude.md — do NOT overwrite the existing content.

**Part C: Add schema reference to vault-level CLAUDE.md.**

Append a new section to `/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/05_AI_Workspace/CLAUDE.md` (the vault CLAUDE.md):

```
## YAML Schema

Canonical YAML frontmatter schema: See the project's `.planning/phases/01-foundation/yaml-frontmatter-schema.md` for all field definitions, types, and allowed values.
```

IMPORTANT: Use Edit tool to append to the vault CLAUDE.md — do NOT overwrite the existing content.
  </action>
  <verify>
  1. Read the dashboard file — confirm it contains valid Dataview query blocks
  2. Read `.claude/claude.md` — confirm the schema reference section was appended
  3. Read vault CLAUDE.md (`05_AI_Workspace/CLAUDE.md`) — confirm the YAML Schema section was appended
  4. Open the dashboard in Obsidian to visually confirm Dataview renders (human verification — note in summary)
  </verify>
  <done>Dashboard note exists with at least one live Dataview query. Both project CLAUDE.md and vault CLAUDE.md reference the canonical schema location.</done>
</task>

</tasks>

<verification>
1. Read `.planning/phases/01-foundation/yaml-frontmatter-schema.md` — confirm all three note types defined with field names, types, and enums
2. Read the dashboard file — confirm valid Dataview DQL blocks present
3. Read `.claude/claude.md` — confirm schema reference section added
4. Read vault CLAUDE.md (`05_AI_Workspace/CLAUDE.md`) — confirm YAML Schema section added
5. Verify schema preserves existing fields: `type`, `title`, `due_date`, `status`, `tags`, `created`, `name`, `next_action`, `context`, `follow_ups`, `last_touched`
</verification>

<success_criteria>
- Canonical YAML schema document covers task, project, and person note types with complete field definitions
- All existing template fields are preserved (additive schema, no breaking changes)
- Date typing rules explicitly documented (no-quotes rule)
- Skeleton dashboard in 05_AI_Workspace/dashboards/ contains at least one Dataview DQL query
- Both project CLAUDE.md and vault CLAUDE.md (05_AI_Workspace/CLAUDE.md) reference the schema document location
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
