---
phase: 03-core-daily-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/surfacing/daily-digest/skill.md
  - .claude/commands/today.md
autonomous: true

must_haves:
  truths:
    - "Invoking the morning briefing skill produces a dated briefing note in 05_AI_Workspace/daily-briefs/ with summary header, task list, and automation opportunities"
    - "The briefing note has Dataview-compatible frontmatter (type: daily-brief, bare ISO date)"
    - "Empty task/people/project directories are handled gracefully (sections skipped, no errors)"
    - "The brief orients first (summary counts) before listing details"
  artifacts:
    - path: ".claude/skills/surfacing/daily-digest/skill.md"
      provides: "Morning briefing skill specification"
      contains: "05_AI_Workspace/daily-briefs"
    - path: ".claude/commands/today.md"
      provides: "Updated /today command pointing to morning briefing skill"
      contains: "daily-brief"
  key_links:
    - from: ".claude/skills/surfacing/daily-digest/skill.md"
      to: "tasks/*.md"
      via: "Claude reads task files for due_date, status, project fields"
    - from: ".claude/skills/surfacing/daily-digest/skill.md"
      to: "05_AI_Workspace/daily-briefs/YYYY-MM-DD-daily-brief.md"
      via: "Write tool creates dated briefing note in vault"
---

<objective>
Replace the existing daily-digest skill with a morning briefing skill that generates a structured daily brief note in the vault and injects a summary into session context.

Purpose: The morning briefing is the start-of-day ritual. It orients the user with overdue/due/follow-up counts before listing details, and offers actionable automation opportunities. The vault note creates a persistent daily record that the EOD update (Plan 03-02) will append to.

Output: Rewritten `skill.md` for the daily-digest skill, and updated `/today` command.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-daily-skills/03-RESEARCH.md
@.planning/phases/03-core-daily-skills/03-CONTEXT.md
@.planning/phases/01-foundation/yaml-frontmatter-schema.md
@.claude/skills/surfacing/daily-digest/skill.md
@.claude/commands/today.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite daily-digest skill.md as morning briefing</name>
  <files>.claude/skills/surfacing/daily-digest/skill.md</files>
  <action>
Replace the entire content of `.claude/skills/surfacing/daily-digest/skill.md` with the new morning briefing specification. The skill instructs Claude on how to generate the daily brief. This is an instruction file, not code.

The new skill.md must specify:

**Triggers:**
- `/today`
- "morning briefing"
- "what's on today"
- "daily brief"

**Data sources Claude reads directly:**
- `tasks/*.md` — extract `due_date`, `status`, `project`, `title` from frontmatter (skip README.md)
- `people/*.md` — extract `follow_up_date`, `name` from frontmatter (skip README.md)
- `projects/*.md` — extract `status`, `health`, `next_action`, `name` from frontmatter (skip README.md)

**Graceful empty handling:** If a directory has only README.md or no files, skip that section entirely. Do not error.

**Output — vault note:**
Write to `05_AI_Workspace/daily-briefs/YYYY-MM-DD-daily-brief.md` (using today's date).

Frontmatter:
```yaml
---
type: daily-brief
date: YYYY-MM-DD
generated_by: claude
---
```

Note: `date` must be bare ISO format (no quotes) per schema rules.

**Note structure (locked decisions from CONTEXT.md):**

```markdown
# Daily Brief — YYYY-MM-DD

## At a Glance
X tasks due today · Y overdue · Z follow-ups needing attention

## Overdue Tasks
| Task | Status | Project | Due |
|------|--------|---------|-----|
(sorted oldest first; omit section if none)

## Due Today
| Task | Status | Project |
|------|--------|---------|
(omit section if none)

## Upcoming (Next 7 Days)
| Task | Status | Project | Due |
|------|--------|---------|-----|
(omit section if none)

## Follow-ups
| Person | Follow-up Date |
|--------|---------------|
(people where follow_up_date <= today + 7 days; omit section if none)

## Active Projects
| Project | Health | Next Action |
|---------|--------|-------------|
(projects where status = active; omit section if none)

## Automation Opportunities
- [Actionable offers based on current task/people state]
  Examples: "3 tasks marked `waiting` — want me to draft follow-ups?"
  "2 tasks have no due date — want me to help prioritize?"
  Only list opportunities where Claude could act RIGHT NOW.
  Skip if no actionable opportunities exist.

---
<!-- Day Summary will be appended by session-end hook -->
```

**Output — session context:**
After writing the vault note, provide a conversational summary inline:
- Lead with the "At a Glance" counts
- List top 3-5 most urgent items (overdue first, then due today)
- State automation opportunities as offers
- End with: "Want me to start on any of these, or dive into something else?"

**Overwrite rule (from AI workspace CLAUDE.md):** If a brief for today already exists, overwrite it (daily-briefs are idempotent — one file per day).

**Anti-patterns to avoid:**
- Do NOT write to `daily/` (that's the old location in project root)
- Do NOT send Slack DM (deferred to later phase)
- Do NOT pre-compute Dataview tables (the vault note is static markdown, not Dataview queries)
- Do NOT include tasks with status `done`
- Do NOT quote dates in frontmatter
  </action>
  <verify>
1. `cat .claude/skills/surfacing/daily-digest/skill.md` shows the new morning briefing content
2. `grep '05_AI_Workspace/daily-briefs' .claude/skills/surfacing/daily-digest/skill.md` confirms vault output path
3. `grep 'At a Glance' .claude/skills/surfacing/daily-digest/skill.md` confirms orient-first structure
4. `grep 'Automation Opportunities' .claude/skills/surfacing/daily-digest/skill.md` confirms actionable offers section
5. `grep -c 'daily/' .claude/skills/surfacing/daily-digest/skill.md` returns 0 or only appears in "daily-briefs" context (old path removed)
6. `grep 'Slack' .claude/skills/surfacing/daily-digest/skill.md` returns nothing (Slack DM deferred)
  </verify>
  <done>daily-digest/skill.md is rewritten as the morning briefing skill: reads tasks/people/projects, writes structured daily-brief note to 05_AI_Workspace/daily-briefs/YYYY-MM-DD-daily-brief.md with orient-first summary, task tables, follow-ups, active projects, and actionable automation opportunities. Handles empty directories gracefully. Does not write to daily/ or send Slack DMs.</done>
</task>

<task type="auto">
  <name>Task 2: Update /today command to point to morning briefing</name>
  <files>.claude/commands/today.md</files>
  <action>
Replace the content of `.claude/commands/today.md` to serve as the entry point for the morning briefing skill.

The updated command should:

1. Reference the daily-digest skill as the primary action
2. Remove the Slack DM step (deferred to later phase)
3. Remove the inbox processing step (that's a separate skill)
4. Keep it focused: the `/today` command = run the morning briefing skill

New content for `today.md`:

```markdown
# /today — Morning Briefing

Run the morning briefing to start your day.

## What It Does

1. **Scan tasks, people, and projects** for overdue items, items due today, upcoming deadlines, and follow-ups
2. **Write a daily brief note** to `05_AI_Workspace/daily-briefs/YYYY-MM-DD-daily-brief.md`
3. **Summarize in session** with orient-first counts and top priorities
4. **Offer automation opportunities** — things Claude can act on right now

## How to Use

Just run `/today` at the start of any session. The briefing is also triggered by:
- "morning briefing"
- "what's on today"
- "daily brief"

## Skill Reference

This command invokes the **daily-digest** skill at:
`.claude/skills/surfacing/daily-digest/skill.md`
```

This keeps the command clean and focused. The skill.md has all the implementation detail.
  </action>
  <verify>
1. `cat .claude/commands/today.md` shows updated content
2. `grep 'daily-digest' .claude/commands/today.md` confirms skill reference
3. `grep -i 'slack' .claude/commands/today.md` returns nothing (Slack deferred)
4. `grep 'inbox' .claude/commands/today.md` returns nothing (separate concern)
5. `wc -l .claude/commands/today.md` is under 30 lines (concise command file)
  </verify>
  <done>/today command is updated to invoke the morning briefing skill. Slack DM and inbox processing removed (deferred/separate concerns). Command is clean and focused on the daily brief.</done>
</task>

</tasks>

<verification>
1. `.claude/skills/surfacing/daily-digest/skill.md` contains the full morning briefing specification
2. `.claude/commands/today.md` references the daily-digest skill
3. No references to writing to `daily/` directory (old path)
4. No references to Slack DM
5. Morning brief note structure includes: At a Glance, Overdue Tasks, Due Today, Upcoming, Follow-ups, Active Projects, Automation Opportunities
6. Frontmatter template uses bare ISO dates (no quotes)
7. Empty directory handling is specified (skip section, no error)
</verification>

<success_criteria>
- The daily-digest skill.md specifies a complete morning briefing that writes to 05_AI_Workspace/daily-briefs/
- The brief orients first with summary counts before listing details
- Automation opportunities are actionable offers, not passive observations
- /today command cleanly invokes the morning briefing skill
- Empty directories handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-daily-skills/03-01-SUMMARY.md`
</output>
