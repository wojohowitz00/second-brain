---
phase: 04-proactive-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/surfacing/daily-digest/skill.md
  - .claude/commands/today.md
autonomous: true

must_haves:
  truths:
    - "Overdue tasks trigger Slack post + macOS notification + daily brief append"
    - "Due-today tasks trigger macOS notification + daily brief append only"
    - "Stale follow-ups (people/waiting, 7+ days) trigger daily brief append only"
    - "No more than 5 alert items surface per morning briefing"
    - "Priority when capped: overdue first, then due-today, then stale"
    - "Alert routing happens ONLY from /today skill, NOT from session-start.sh"
  artifacts:
    - path: ".claude/skills/surfacing/daily-digest/skill.md"
      provides: "Updated morning briefing with alert routing logic"
      contains: "Alert Routing"
    - path: ".claude/commands/today.md"
      provides: "Updated /today command documenting alert behavior"
      contains: "alert"
  key_links:
    - from: ".claude/skills/surfacing/daily-digest/skill.md"
      to: "backend/_scripts/slack_client.py"
      via: "Slack posting for overdue alerts"
      pattern: "slack_client"
    - from: ".claude/skills/surfacing/daily-digest/skill.md"
      to: "osascript"
      via: "macOS notification for overdue and due-today"
      pattern: "osascript"
---

<objective>
Add severity-based alert routing to the morning briefing skill. Overdue items get full three-channel delivery (Slack + macOS notification + daily brief), due-today items get macOS + daily brief, and stale follow-ups get daily brief only. Capped at 5 items.

Purpose: Urgent items reach the user through the right channel at the right urgency level, preventing both missed alerts and alert fatigue.

Output: Updated daily-digest skill with alert routing logic, updated /today command documentation.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/surfacing/daily-digest/skill.md
@.claude/commands/today.md
@.claude/hooks/session-start.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alert routing to daily-digest skill</name>
  <files>.claude/skills/surfacing/daily-digest/skill.md</files>
  <action>
Read the existing daily-digest skill file first. Add a new workflow step AFTER the vault note is written (after Step 2, before Step 3 "Inject Session Summary"). This new step performs external alert delivery.

Add the following step:

### 2.5. Alert Routing (External Delivery)

After writing the daily brief vault note, deliver urgent items through external channels based on severity.

**Urgent item criteria:**
- **Overdue:** tasks with `due_date` < today AND `status` != done
- **Due today:** tasks with `due_date` = today AND `status` != done
- **Stale follow-ups:** people notes with `follow_up_date` <= today AND file mtime older than 7 days (use `stat -f "%Sm" -t "%s"` on macOS, compare against 7-day threshold)

**5-item cap with priority ordering:**
1. Collect all urgent items
2. Sort: overdue first (oldest due_date first), then due-today, then stale follow-ups
3. Take first 5 items only
4. If more than 5 exist, note the overflow count in the daily brief

**Severity-based routing:**

| Severity | Slack | macOS Notification | Daily Brief |
|----------|-------|--------------------|-------------|
| Overdue | YES | YES | YES (already in note) |
| Due today | NO | YES | YES (already in note) |
| Stale follow-up | NO | NO | YES (already in note) |

**Slack delivery (overdue items only):**
```bash
source /Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/backend/_scripts/.env
python3 -c "
import sys
sys.path.insert(0, '/Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/backend/_scripts')
from slack_client import post_message
import os
post_message(channel=os.environ['SLACK_CHANNEL_ID'], text='FORMATTED_ALERT_TEXT')
"
```

Format Slack message as:
```
:rotating_light: Second Brain Alert — N overdue items
- Task Name (due YYYY-MM-DD, N days overdue)
- Task Name (due YYYY-MM-DD, N days overdue)
```

**macOS notification (overdue + due-today items):**
```bash
osascript -e "display notification \"N items need attention\" with title \"Second Brain\" subtitle \"Morning Alert\""
```

Use a single consolidated notification (not one per item). Subtitle should summarize: "N overdue, M due today" or similar.

**Error handling:**
- Slack post fails (network, auth): log warning in session, continue. Do NOT block the briefing.
- osascript fails: log warning, continue.
- No urgent items: skip alert routing entirely (no empty Slack posts, no empty notifications).

**CRITICAL anti-pattern:** session-start.sh remains context-injection ONLY. It must NOT be modified to do external delivery. All Slack/osascript delivery happens in THIS skill step only. This prevents double-firing (research pitfall #1).

Also update the "Anti-Patterns" section of the skill to include:
- Do NOT add alert routing to session-start.sh (that script is context-injection only)
- Do NOT send more than 5 alert items regardless of how many urgent items exist
- Do NOT send Slack messages for due-today or stale items (only overdue)
  </action>
  <verify>Read the updated skill file. Confirm: (1) alert routing step exists between vault write and session summary, (2) three severity tiers with correct channel mapping, (3) 5-item cap with priority ordering, (4) Slack code snippet uses correct path and env var, (5) osascript command present, (6) anti-pattern about session-start.sh documented, (7) error handling for failed delivery.</verify>
  <done>Daily-digest skill has alert routing with three severity tiers, correct channel mapping, 5-item cap, and explicit anti-pattern preventing session-start.sh modification.</done>
</task>

<task type="auto">
  <name>Task 2: Update /today command documentation</name>
  <files>.claude/commands/today.md</files>
  <action>
Read the existing /today command file. Update it to document the new alert routing behavior.

In the "What It Does" section, add a step:
```
5. **Route alerts by severity** — overdue items post to Slack and trigger macOS notification; due-today items trigger macOS notification only; stale follow-ups appear in daily brief only
```

Add a new section:

```
## Alert Routing

The morning briefing now delivers urgent items through external channels:

| Item Type | Slack | macOS Notification | Daily Brief |
|-----------|-------|--------------------|-------------|
| Overdue task | Yes | Yes | Yes |
| Due today | No | Yes | Yes |
| Stale follow-up | No | No | Yes |

- Maximum 5 alert items per briefing (overdue prioritized)
- Slack alerts go to the configured channel (SLACK_CHANNEL_ID in backend/.env)
- Delivery failures are logged but don't block the briefing
```
  </action>
  <verify>Read the updated command file. Confirm it documents the three-tier routing table, 5-item cap, and Slack channel reference.</verify>
  <done>/today command documents alert routing behavior with severity tiers and volume cap.</done>
</task>

</tasks>

<verification>
1. Daily-digest skill has alert routing step with three severity tiers
2. Overdue -> Slack + macOS + daily brief; Due today -> macOS + daily brief; Stale -> daily brief only
3. 5-item cap with overdue-first priority ordering
4. Slack posting uses `slack_client.post_message` with `SLACK_CHANNEL_ID` env var
5. macOS uses `osascript` with consolidated single notification
6. session-start.sh is NOT modified (delivery only from /today skill)
7. Error handling: delivery failures logged but don't block briefing
8. /today command documents alert behavior
</verification>

<success_criteria>
- Alert routing logic is fully specified in the daily-digest skill
- Three severity tiers with correct channel mapping documented
- 5-item cap prevents alert fatigue
- session-start.sh remains untouched (context injection only)
- /today command updated to reflect new behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-proactive-layer/04-02-SUMMARY.md`
</output>
