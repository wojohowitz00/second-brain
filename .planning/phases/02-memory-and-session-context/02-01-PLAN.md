---
phase: 02-memory-and-session-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md
autonomous: true

must_haves:
  truths:
    - "Claude Code memory is initialized with vault conventions, user preferences, and reference pointers"
    - "Context is available at the start of a fresh session without manual re-loading"
    - "After a context compaction event, critical vault conventions are re-injected"
  artifacts:
    - path: "~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md"
      provides: "Persistent cross-session memory with vault profile, conventions, and reference pointers"
      max_lines: 200
  key_links:
    - from: "MEMORY.md"
      to: "CLAUDE.md files"
      via: "complementary roles — MEMORY.md stores learned facts, CLAUDE.md stores rules"
    - from: "MEMORY.md vault profile section"
      to: "vault filesystem paths"
      via: "hardcoded absolute paths for vault root, tasks dir, AI workspace"
---

<objective>
Initialize Claude Code persistent memory with vault profile, conventions, and reference pointers so Claude has full working context from the first interaction in any new session.

Purpose: Without seeded memory, every new Claude Code session starts from zero — the user must re-explain vault structure, write boundaries, date format rules, and task conventions. This plan eliminates that cold-start problem.

Output: A MEMORY.md file at the Claude Code auto-memory path, under 200 lines, containing vault profile, key reference files, system architecture, and empty sections for preferences and patterns that Claude will populate over time.
</objective>

<execution_context>
@/Users/richardyu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardyu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-and-session-context/02-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create memory directory and seed MEMORY.md</name>
  <files>~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md</files>
  <action>
1. Create the memory directory:
   ```bash
   mkdir -p ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory
   ```

2. Write MEMORY.md with the following exact content (keep under 200 lines total):

```markdown
# Second Brain -- Memory

Last updated: 2026-03-14

## Vault Profile

- Vault root: /Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/
- AI workspace: 05_AI_Workspace/ (ONLY folder Claude may write to in vault)
- PARA folders: 01_Projects, 02_Areas_of_Interest, 03_Research, 04_Archive (human-only, write-guarded by PreToolUse hook)
- Tasks dir: /Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/tasks/ (project root, NOT in vault)
- Projects dir: /Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/projects/
- Daily notes: daily/ (project root)
- People notes: people/ (project root)
- Ideas: ideas/ (project root)

## Key Reference Files

- YAML frontmatter schema: .planning/phases/01-foundation/yaml-frontmatter-schema.md
- Vault write guard hook: .claude/hooks/vault-write-guard.py
- Project-level settings: .claude/settings.json
- Task SOP: docs/sop/tasks.md

## Conventions

- Task status enums: backlog | active | waiting | blocked | done | someday
- Date format: bare ISO YYYY-MM-DD, NEVER quoted (quoted dates become TEXT in Dataview, breaking date queries)
- Tags: #active, #waiting, #blocked, #someday, #done, #backlog (status); #sales, #content, #product, #admin, #research, #people (domain); #quick, #deep, #collab (context)
- All AI-generated notes with structured data MUST use field names and types from the canonical schema
- New schema fields must be added to schema before use (additive only, no renames)

## System Architecture

- Python backend handles: Slack polling -> Ollama classification -> vault filing (DO NOT MODIFY without explicit instruction)
- Claude Code handles: morning ritual, tasks, writing, research, session context
- Hook system: PreToolUse blocks writes to folders 01_ through 04_ in vault
- Sessions: SessionStart surfaces stale tasks; SessionEnd refreshes dashboards
- canvas/ subfolder gated behind Phase 4 research -- do not use yet

## User Preferences

(Claude updates this section as preferences are discovered during sessions)

## Patterns Learned

(Claude updates this section as patterns emerge from repeated interactions)
```

IMPORTANT: The file must be UNDER 200 lines. Claude Code auto-loads only the first 200 lines of MEMORY.md. Count the lines before writing. The content above is approximately 45 lines, well within budget.

Do NOT use pyyaml or any external dependencies. This is a plain markdown file created via Write tool or bash heredoc.
  </action>
  <verify>
1. Confirm file exists: `ls -la ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md`
2. Confirm line count is under 200: `wc -l ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md`
3. Confirm key sections present: `grep -c '## ' ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md` should return 6 (Vault Profile, Key Reference Files, Conventions, System Architecture, User Preferences, Patterns Learned)
4. Confirm vault root path is correct: `grep 'Vault root' ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md`
  </verify>
  <done>MEMORY.md exists at the auto-memory path, contains vault profile with correct absolute paths, key references, conventions (date format, status enums, tag taxonomy), system architecture summary, and empty preference/pattern sections. File is under 200 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Verify auto-load behavior and post-compaction re-injection</name>
  <files>~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md</files>
  <action>
Verify the memory file will be auto-loaded correctly:

1. Confirm the file path matches Claude Code's auto-memory convention. The path must be:
   `~/.claude/projects/{sanitized-project-path}/memory/MEMORY.md`
   where the sanitized path replaces `/` with `-` from the project root.

2. Confirm the project directory name matches what Claude Code uses. Run:
   ```bash
   ls ~/.claude/projects/ | grep second-brain
   ```
   The directory `-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain` must exist (verified in research).

3. Verify MEMORY.md content quality:
   - All absolute paths in the file must resolve to real directories:
     ```bash
     test -d "/Users/richardyu/Library/Mobile Documents/iCloud~md~obsidian/Documents/Home/" && echo "vault OK"
     test -d "/Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/tasks/" && echo "tasks OK"
     test -f "/Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/.claude/hooks/vault-write-guard.py" && echo "hook OK"
     test -f "/Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/.planning/phases/01-foundation/yaml-frontmatter-schema.md" && echo "schema OK"
     ```

4. Post-compaction behavior relies on two mechanisms (no additional code needed):
   - CLAUDE.md is re-read in full after compaction (automatic)
   - MEMORY.md first 200 lines are re-loaded after compaction (automatic)
   - SessionStart fires with `source: "compact"` after compaction (will inject stale task context via Plan 02-02's hook)

   Document this in MEMORY.md by confirming the "System Architecture" section mentions session hooks.

No code changes needed in this task -- it is a verification and path-validation step.
  </action>
  <verify>
1. All four path tests pass (vault OK, tasks OK, hook OK, schema OK)
2. `wc -l` confirms under 200 lines
3. MEMORY.md "System Architecture" section mentions SessionStart and SessionEnd
  </verify>
  <done>MEMORY.md is confirmed at the correct auto-memory path, all referenced paths resolve to real files/directories, content is under 200 lines, and post-compaction re-injection requires no additional code (handled by Claude Code's built-in CLAUDE.md + MEMORY.md reload).</done>
</task>

</tasks>

<verification>
1. `ls ~/.claude/projects/-Users-richardyu-PARA-01-Projects-Personal-apps-second-brain/memory/MEMORY.md` -- file exists
2. `wc -l` on MEMORY.md returns a number under 200
3. `grep '## ' MEMORY.md` returns 6 section headers
4. All absolute paths referenced in MEMORY.md resolve to real filesystem locations
5. No external dependencies installed (no pip, no npm)
</verification>

<success_criteria>
- MEMORY.md exists at Claude Code auto-memory path with vault profile, conventions, references, and architecture
- File is under 200 lines (auto-load limit)
- All filesystem paths in the file are valid
- Post-compaction re-injection requires no additional code
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-and-session-context/02-01-SUMMARY.md`
</output>
