# Phase 9.1: Packaging Cleanup & Polish

**Goal**: Professionalize the application packaging by removing legacy configuration (`py2app`), ensuring a clean build with PyInstaller, and adding a custom application icon.

## User Review Required

> [!IMPORTANT]
> **Breaking Change**: This plan permanently removes `setup.py` and the `py2app` dependency. Future builds must use `scripts/build_app.sh` (PyInstaller).

> [!NOTE]
> **App Icon**: A placeholder icon will be generated. The user can replace `resources/icon.icns` with a custom design later.

## Proposed Changes

### Configuration Cleanup
#### [DELETE] [setup.py](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/setup.py)
- Remove legacy build script.

#### [MODIFY] [backend/pyproject.toml](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/backend/pyproject.toml)
- Remove `py2app` from dependencies.
- Ensure `pyinstaller` is pinned.

### Branding
#### [NEW] [resources/icon.icns](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/resources/icon.icns)
- Generate a basic `.icns` file using standard macOS tools (`sips`, `iconutil`) from a generated or stock image to replace the default Python rocket icon.
- *Implementation Detail*: Will create a temporary `icon.iconset` directory, generate PNGs of various sizes, and convert to `.icns`.

#### [MODIFY] [SecondBrain.spec](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/SecondBrain.spec)
- Update `BUNDLE` call to include `icon='resources/icon.icns'`.
- Verify `info_plist` metadata (version, copyright).

### Documentation
#### [MODIFY] [scripts/build_app.sh](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/scripts/build_app.sh)
- Add comment or check to ensure `icon.icns` exists before building.

## Verification Plan

### Automated Verification
- Run `./scripts/build_app.sh`
    - **Expect**: Build succeeds without `py2app` errors.
    - **Expect**: `dist/Second Brain.app` is created.
    - **Expect**: `dist/Second Brain.app/Contents/Resources/icon.icns` exists.

### Manual Verification
1.  **Icon Inspection**:
    - Open `dist/` in Finder.
    - Confirm "Second Brain.app" shows the new icon (not the default executable icon).
    - Get Info (Cmd+I) on the app to verify copyright/version strings.
2.  **Launch Test**:
    - Open the app: `open dist/Second Brain.app`
    - Verify menu bar icon appears and app functions normally.
3.  **Clean Environment**:
    - Verify `py2app` is not required for a fresh build.
