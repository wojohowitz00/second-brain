# Apps

Current runnable implementation tracks live here.

- `backend/` — Python capture and processing pipeline
- `apple/` — macOS Swift package implementation
- `ios/` — lightweight Xcode app track
