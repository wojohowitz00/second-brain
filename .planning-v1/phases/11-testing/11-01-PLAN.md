# Phase 11: Testing Suite

**Goal**: Establish a robust testing foundation for the Swift application, moving beyond unit tests to integration and UI verification.

## User Review Required

> [!NOTE]
> This phase adds a new testing target and configuration changes to the Xcode project/Package.swift.

## Proposed Changes

### 1. Integration Tests
#### [NEW] [apple/SecondBrain/Tests/SecondBrainTests/Integration/NoteProcessorIntegrationTests.swift](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/Tests/SecondBrainTests/Integration/NoteProcessorIntegrationTests.swift)
- **Goal**: Verify the end-to-end flow from "Mock Slack" -> "Mock Ollama" -> "Real File System".
- **Implementation**:
    - Mock `SlackClient` to return controlled messages.
    - Mock `OllamaClient` to return controlled classification.
    - Use `VaultScanner` with a temporary directory.
    - **Assert**: Correct files are created in the correct folders (Projects/Areas/Resources/Inbox).

### 2. Dependency Injection Refactor
#### [MODIFY] [apple/SecondBrain/Sources/SecondBrain/Services/](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/Sources/SecondBrain/Services/)
- **Refactor**: Protocolize `SlackClient` and `OllamaClient` to allow for clean mocking in integration tests (`SlackClientProtocol`, `OllamaClientProtocol`).
- **Update**: `NoteProcessor` to accept protocols.

### 3. Code Coverage
#### [CONFIG] [apple/SecondBrain/Package.swift](file:///Users/richardyu/PARA/01_Projects/Personal/apps/second-brain/apple/SecondBrain/Package.swift)
- **Action**: Enable code coverage gathering when running tests.
- **Goal**: Aim for >80% coverage on Core Services.

## Verification Plan

### Automated Tests
*   **Run**: `swift test --enable-code-coverage`
*   **Check**:
    *   All existing unit tests pass.
    *   New `NoteProcessorIntegrationTests` pass.
    *   Coverage report shows high coverage for `NoteProcessor` and `VaultScanner`.

### Manual Verification
*   **UI**: Manually verify Settings View persistence (as automated UI tests for Menu Bar apps are complex/flaky without XCTest UI Runner which is harder in pure SwiftPM).
